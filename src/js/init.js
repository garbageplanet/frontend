document.addEventListener("DOMContentLoaded", function () {
    
  // Init these blocks in order

  maps.init();

  features.init();

  ui.init();

  actions.init();

  session.init();
    
  touring.init();

});
